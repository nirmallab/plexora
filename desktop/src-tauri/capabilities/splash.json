{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splash",
  "description": "The startup splash, bundled with the app.",
  "windows": ["splash"],
  "permissions": ["core:event:default"]
}
